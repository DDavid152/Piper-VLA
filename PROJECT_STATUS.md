# Piper-VLA 项目状态

## 当前阶段（2026-08-06）

数据采集、clean 数据集、SmolVLA/ACT 两条训练线、v2 主动标定和受控实机部署
均已跑通。当前重点从“控制链能否运行”转为比较两种策略的真实任务效果。

## 已完成

- 环境：Ubuntu 22.04、RTX 4000 Ada 20 GB、Python 3.12、LeRobot v0.6.0、
  PyTorch 2.11.0+cu128。
- 硬件：Piper master/follower 原生遥操、双 Orbbec 640×480@30、1 Mbps CAN
  和 USB-CAN 固定身份均已验收。
- 数据：clean 数据集 51 episodes / 42,066 帧，自动 QA 51/51 通过。
- SmolVLA：batch 32、10K 步；最佳 `004000`，验证损失 0.1572。同步影子推理
  时序较慢，RTC 后续完成 30 秒主动控制诊断，但抓取/完整任务表现不佳。
- ACT：batch 8、100K 步、52M 参数；最佳 `070000`，验证损失 0.1225。
- ACT 实机：50/50 动作发送成功；三次有效 30 秒运行分别发送 850、845、848
  条动作，均由时间预算正常停止且 `fault=None`。另一次日志重用导致启动前
  `FileExistsError`，不计为运行失败。
- 控制频率：ACT 动作目标约 28.2～28.3 Hz；每次推理输出 50 步，因此网络
  视觉闭环更新约 0.57 Hz，无额外插值。
- 主动安全：v2 标定验证通过；物理限位、p99 限速、动态位移窗口、CAN/相机/
  反馈看门狗、动作/时间预算和单次急停均已在实机路径启用。

## 当前结论

- 两种策略均已证明可以加载双相机与 7 维状态并驱动真实控制链。
- ACT 训练与部署吞吐稳定，但真实任务成功率仍需按相同摆放重复记录；技术运行
  成功不等于抓袋任务成功。
- ACT 与 SmolVLA 的损失函数不同，验证 loss 数值不能直接横向排序；应比较相同
  起始条件下抓取、抬升、保持、放回和释放的完整成功率。
- ACT 当前一次推理后开环执行约 1.7 秒。若视觉纠偏不足，可只调整
  `n_action_steps` 做更短执行窗口实验，但必须重新测量推理停顿与动作频率。

## 下一步

1. 使用 `scripts/run_piper_act_rollout.sh --profile 30s` 重复 ACT 测试，逐次记录
   是否完成五阶段任务以及警告数量。
2. 在相同袋子摆放、相机和起始姿态下复测 SmolVLA，形成可比较成功率。
3. 若 ACT 抓取阶段失败，优先检查夹爪初始开度、提带位置和示教起始分布；若
   动作阶段滞后，检查模型 50 步开环窗口与机械臂速度限制。
