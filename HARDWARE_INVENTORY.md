# 硬件清单

本文件记录新 Ubuntu 主机上的只读硬件审计结果，不沿用旧电脑的设备枚举、
序列号或固件结论。

本文提到的历史 `logs/...` 证据已于 2026-07-30 按原相对路径移到本机归档
`/home/ubuntu22/Piper-VLA-local-archive/2026-07-30/`，不再位于项目目录。
归档完整性见其中的 `MANIFEST.sha256`。

## 软件配置阶段结果

2026-07-28 使用 pyorbbecsdk2 2.0.18 仅调用设备枚举接口，检测到
Orbbec 设备 0 台。没有启动相机流，也没有执行固件升级。

Piper SDK 仅验证 Python 导入；没有打开 CAN、创建控制接口、发送报文或使能
机械臂。因此当前没有可信的 Piper 或 USB-CAN 设备结论。

## 2026-07-29 只读硬件识别

### Piper 与 USB-CAN

| 角色 | 单独连接时的临时接口 | USB-CAN 序列号 | 当前结论 |
|---|---|---|---|
| master/leader | `can0`（单独测试枚举） | `0033002F5547571120343930` | 已重新写入示教输入臂角色；断电持久化、实机遥操和动态控制帧验证通过 |
| follower/slave | `can0` | `002900225547571120343930` | 已通过官方上位机配置为从臂；被动 CAN 与 SDK 验证通过 |

`can0`/`can1` 会受插入顺序影响，软件不以接口名本身判断身份。Piper
Robot、Teleoperator 和实时网页在打开接口前都会沿 sysfs 核对 USB-CAN
序列号；不匹配 `002900225547571120343930` 时拒绝连接。
最终控制链采用 Piper 官方原生主从方案。两臂角色此前已经由官方上位机
配置；由于计划 master 单独测试时仍表现为普通反馈臂，经用户明确授权后已
重新写入一次 master 角色。最终两臂接入同一 CAN 总线，使用一个 USB-CAN
接入电脑。

follower 于 2026-07-29 单独连接验证：

- 经典 CAN 1 Mbps，接口为 `ERROR-ACTIVE`；
- 5 秒收到 15,460 帧，总线错误、仲裁丢失和 bus-off 均为 0；
- 只观察到 follower 反馈类 CAN ID，未出现 master 的
  `0x151`、`0x155`～`0x159` 控制帧；
- Piper SDK 使用 `piper_init=False` 纯接收，状态、关节和夹爪反馈均为
  200 Hz；
- 机械臂状态正常，六关节通信错误和角度限位错误均为 false；
- 当前关节角约为 `[-1.508, 0, 0, 2.806, 23.325, -2.759]°`；
- 夹爪位置和力矩反馈均为 0，未使能、未回零，但无驱动错误；
- 测试期间 CAN TX 为 0，结束后接口恢复为 DOWN。

本次未发送固件查询，因此 follower 固件版本仍待后续只读查询确认。

计划 master 于 2026-07-29 单独连接验证：

- 经典 CAN 1 Mbps，接口为 `ERROR-ACTIVE`；
- 5 秒收到 15,695 帧，总线错误、仲裁丢失和 bus-off 均为 0；
- 只观察到普通反馈类 CAN ID，未出现 master 的
  `0x151`、`0x155`～`0x159` 控制帧；
- Piper SDK 纯接收结果为状态、关节和夹爪反馈 200 Hz，
  `ctrl_mode=0x00`，关节、夹爪及 `0x151` 控制帧均为 0 Hz；
- 测试期间 TX 为 0，结束后接口恢复为 DOWN。

因此实体臂和 USB-CAN 映射已确认，但上位机曾写入的 master 角色当前没有在
运行时生效。项目必须在明确确认后单独重写或由官方上位机重新设置
“示教输入臂”，随后重新断电上电并复验；在此之前禁止合并总线遥操作。

用户完成现场安全确认并明确授权后，已于 2026-07-29 对唯一连接且序列号为
`0033002F5547571120343930` 的适配器发送一次：

```text
MasterSlaveConfig(0xFA, 0x00, 0x00, 0x00)
```

发送前 CAN TX 为 0，发送后为 1 帧、8 字节，没有发送其他指令。配置后普通
反馈立即停止，随后 5 秒接收 0 帧，说明机械臂已退出原来的 slave/常规反馈
状态，但尚未观察到 master 控制帧。

归档原始日志：`logs/master_role_config_20260729_120425.log`。

master 断电重启后，于 2026-07-29 再次完成 5 秒纯监听：

- USB-CAN 序列号仍为 `0033002F5547571120343930`；
- 监听前后 RX 均为 2,234 帧，TX 均为 1 帧，增量均为 0；
- TX 中已有的 1 帧是本次断电前发送的角色配置帧，本轮没有新发送报文；
- 原来的普通反馈没有恢复，说明角色配置没有表现出回退；
- master 单独连接时仍未发出 `0x151`、`0x155`～`0x159` 控制帧；
- CAN 保持 `ERROR-ACTIVE` 且错误计数为 0，结束后恢复为 DOWN。

Piper 官方双臂流程要求两臂共享 CAN，并按 follower 先上电、master 后上电的
顺序启动。因此，单独 master 的零报文结果不能判定角色配置失败；下一步应
在现场安全确认和用户明确授权后，按官方接线合并总线并先做被动报文验收。

归档空抓包：`logs/candump-2026-07-29_121237.log`。
归档验收摘要：`logs/master_powercycle_passive_20260729_121237_summary.log`。

两臂随后按官方接线共享 CAN，用户报告先上电 follower、再上电 master。
2026-07-29 使用序列号 `002900225547571120343930` 的单个 USB-CAN 完成
约 8 秒纯监听：

- 共收到 25,121 帧，平均约 3,141.82 帧/秒，主机 TX 增量为 0；
- CAN 保持 `ERROR-ACTIVE`，错误、仲裁丢失和 bus-off 均为 0；
- follower 的 `0x2A1`～`0x2A8` 完整且约为 200 Hz；
- `0x2A1` 始终为 `0100010000000000`：CAN 指令控制、状态正常、
  MOVE J、已到达目标、故障码 0；
- `0x151`、`0x155`、`0x156`、`0x157`、`0x159` 全部为 0；
- 可能带偏移的全部 `0x15x`、`0x16x`、`0x17x` 控制范围也均为 0；
- 结束后接口恢复为 DOWN。

该轮静止监听只证明 USB-CAN 和可见的 follower 总线段健康，当时没有获得
master 控制帧证据。随后用户实际拖动 master，确认 follower 已能正常遥操，
因此主从角色、共享 CAN 和官方上电顺序的实机功能验证通过。静止抓包不能
代表动态控制期间的报文分布。

归档原始抓包：`logs/candump-2026-07-29_140502.log`。
归档验收摘要：`logs/shared_can_passive_20260729_140502_summary.log`。

用户报告遥操正常后，在两臂保持静止时又完成约 8 秒纯监听：

- 收到 25,128 帧，平均约 3,142.28 帧/秒，主机 TX 增量为 0；
- follower 反馈稳定，总线错误均为 0；
- `0x2A1` 始终为 `0100010001000000`：CAN 指令控制、状态正常、
  MOVE J、目标未到达、故障码 0；
- 静止期间仍未观察到 `0x15x`、`0x16x` 或 `0x17x` 控制帧。

该静止抓包随后由动态抓包解释：master 控制帧只集中出现在示教输入变化的
短时间窗口，静止时不持续发送。

归档原始抓包：`logs/candump-2026-07-29_141134.log`。
归档验收摘要：`logs/teleop_static_passive_20260729_141134_summary.log`。

最后在用户执行小幅腕部动作并回位时完成约 120 秒动态纯监听：

- 共收到 377,929 帧，平均约 3,149.55 帧/秒；
- 捕获 `0x151` 45 帧，`0x155`、`0x156`、`0x157`、`0x159`
  各 211 帧，全部集中在五个短动作窗口；
- `0x151` 始终为 `010164AD00000000`，即 CAN 指令、关节控制、
  速度比例 100、高跟随模式 `0xAD`；
- follower 的 `0x2A1`～`0x2A8` 均约 200 Hz，状态正常且故障码为 0；
- 主机 TX 增量为 0，总线错误、仲裁丢失和 bus-off 均为 0；
- 控制目标的最大变化主要在协议 J5：13.508°，同一主要动作窗口内
  follower J5 反馈变化 13.338°；
- 现场称为腕部的动作主要映射为协议 J5，而非 J6，后续 LeRobot 接口必须
  保留该编号映射；
- 结束后 `can0` 恢复为 DOWN。

双 Piper 官方原生 master/follower 遥操至此通过实机与协议双重验收。

归档原始抓包：`logs/candump-2026-07-29_141558.log`。
归档验收摘要：`logs/teleop_dynamic_passive_20260729_141558_summary.log`。

### Orbbec 相机

| 数据键 | 物理角色 | 型号 | 序列号 | 固件 / 硬件 | SDK UID | 连接 |
|---|---|---|---|---|---|---|
| `wrist` | 末端相机 | Gemini 335L | `CP28563000XP` | 1.4.60 / 0.1 | `2-9-7` | USB 3.2，5 Gbps，当前连接 |
| `front` | 远程固定相机 | Gemini 335L | `CP28563000XR` | 1.4.60 / 0.1 | `2-2-5` | USB 3.2，5 Gbps，当前连接 |

映射通过逐台拔插和 pyorbbecsdk2 2.0.18 只读枚举确认，不依赖
`/dev/video*` 编号。两台设备的 VID:PID 均为 `2bc5:0804`。调整后它们位于
同一 xHCI 控制器的不同下游根端口（XP 为端口 9，XR 为端口 2），均以
5 Gbps 链路工作。测试期间没有修改或升级相机固件。

2026-07-29 双相机验收结果：

- 项目采集配置：RGB、640×480、30 FPS、深度关闭；
- 并发 RGB-only 运行 30 秒，两路各 900 个后台帧，均为 30.0 FPS；
- 两路帧号间断、重复帧、硬件时间戳回退、超时、读取失败和坏帧均为 0；
- 两路 RGB+深度并发诊断运行 10 秒，两路各 300 帧，均为 30.0 FPS，
  错误项仍全部为 0；
- 保存的 RGB 快照证明两路均能产生有效非黑图像。

归档原始结果：

- `logs/orbbec_dual_rgb_20260729_144204.json`
- `logs/orbbec_dual_rgb_20260729_144204_snapshots/front_rgb.png`
- `logs/orbbec_dual_rgb_20260729_144204_snapshots/wrist_rgb.png`
- `logs/orbbec_dual_rgb_depth_20260729_144321.json`

2026-07-29 相机固定并调整 USB 后再次验收：

- 两台相机均以 USB 3.2、5 Gbps 枚举；
- 双路 RGB-only 并发运行 30 秒，`front` 获得 899 个后台帧
  （29.97 FPS），`wrist` 获得 900 个后台帧（30.0 FPS）；
- 两路帧号间断、重复帧、时间戳回退、超时、读取失败和坏帧仍全部为 0；
- `front` 已覆盖桌面任务区、物体和机械臂，可作为全局任务视角；
- `wrist` 为机械结构固定的末端前向视角，已覆盖前方桌面，但不包含夹爪
  尖端；正式任务必须位于两路视野重叠区，由 `front` 补充直接接触过程；
- 当前构图与该固定结构限制一并验收，不对图像做软件旋转或伪造。

安装后归档结果：

- `logs/orbbec_post_mount_rgb_20260729_161521.json`
- `logs/orbbec_post_mount_rgb_20260729_161521_snapshots/front_rgb.png`
- `logs/orbbec_post_mount_rgb_20260729_161521_snapshots/wrist_rgb.png`
- `logs/orbbec_post_mount_20260729_161521_summary.log`

## 待核实

- 两台 Piper 的固件和夹爪配置；
- 每次实机运行前的急停与工作空间安全确认。
