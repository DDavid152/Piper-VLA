# 硬件清单

截至 2026-08-06，当前机器人的身份、相机映射和主动控制链如下。

## Piper 与 CAN

| 角色 | USB-CAN 序列号 | 用途 |
|---|---|---|
| follower/slave | `002900225547571120343930` | 采集反馈、接受部署控制；软件以此序列号拒绝错误适配器 |
| master/leader | `0033002F5547571120343930` | 官方原生遥操作和数据采集示教 |

- 接口：经典 CAN、1 Mbps；运行前必须为 `UP`、`ERROR-ACTIVE` 且错误计数为 0。
- `can0` 名称可能随插拔顺序变化，设备身份以 USB 序列号为准。
- 数据采集使用原生 master/follower 共享总线；软件插件只读，主机 TX 应不增长。
- 模型主动部署时 master 必须断电或与控制总线物理隔离，只连接 follower。
- 主动路径只使用 Piper 官方 SDK，不使用 raw CAN、角色重配置、设零或自动回零。

2026-07-29 已完成 master/follower 角色、上电顺序、动态遥操和协议只读验收。
2026-08-05 完成 v2 映射与十二项 ±0.5° commissioning；2026-08-06 ACT 完成
50 动作和三次有效 30 秒运行，均无安全故障。

## 相机

| 数据键 | 安装位置 | 型号 | 序列号 | 当前配置 |
|---|---|---|---|---|
| `front` | 固定外部视角 | Orbbec Gemini 335L | `CP28563000XR` | RGB 640×480@30，USB 3.2 |
| `wrist` | 末端视角 | Orbbec Gemini 335L | `CP28563000XP` | RGB 640×480@30，USB 3.2 |

两路身份始终由序列号确定，不使用 `/dev/video*`。双路 RGB 并发 30 秒各获得
900 帧；无帧号间断、时间戳回退、超时或坏帧。深度仅用于带宽诊断，训练和
部署均为 RGB-only。

## 主机

- Ubuntu 22.04.5 LTS，x86_64，内存 62 GiB。
- NVIDIA RTX 4000 Ada，20,475 MiB 显存。
- 项目所在磁盘足以保留数据集和全部 ACT/SmolVLA checkpoint。

## 主动运行接线边界

1. 停止原生 master 遥操，master 断电或物理隔离。
2. follower 与序列号 `002900225547571120343930` 的适配器连接。
3. 两台相机保持与采集时相同的序列号、位置和 RGB 参数。
4. 操作员持有经过测试的物理急停，工作区无人员和障碍物。
5. 每轮结束后人工检查、恢复急停并重新摆放；禁止无人值守循环。

早期硬件与安装原始日志已归档到
`/home/ubuntu22/Piper-VLA-local-archive/2026-07-30/`，由归档内
`MANIFEST.sha256` 校验。
